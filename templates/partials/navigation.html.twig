<nav class="navbar navbar-expand-lg navbar-light fixed-top" id="mainNav">
    <div class="container">
        <a class="navbar-brand " href="{{ path('index') }}">Null Training T3
            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") and app.user.getTeam is not empty %}
                - {{ app.user.getTeam }}
            {% endif %}
        </a>
        <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse"
                data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false"
                aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link" href="#about">About</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#services">Services</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if app.request.attributes.get('_route') == 'schedule_index' %}active{% endif %}" href="{{ path('schedule_index') }}">Schedule</a>
                </li>
                <li class="nav-item">
                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                        <a href="{{ path('fos_user_security_logout') }}" class="nav-link btn-primary">
                            {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                            <span class="app_username">({{ app.user.username }})</span>
                        </a>
                    {% else %}
                        <a href="{{ path('fos_user_security_login') }}"
                           class="nav-link btn-primary">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a>
                    {% endif %}
                </li>
            </ul>
        </div>
    </div>
</nav>
