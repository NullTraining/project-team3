{% extends 'base.html.twig' %}

{% set bodyClass = 'set-password__page' %}

{% block body %}

    <section id="contact">
        <div class="container">
            <div class="row">
                <div class="col-lg-12 text-center">
                    <h2 class="section-heading text-uppercase">Congrats</h2>
                    <h3 class="section-subheading text-muted">You are ready to go.</h3>
                </div>
            </div>
            <div class="row">
                <div clas="col-sm-12 text-center">
                    <a class="btn btn-primary btn-xl" href="{{ path('index') }}">Go to homepage</a>
                </div>
            </div>
        </div>
    </section>

{% endblock %}
